{
  "name": "ziwon-ai-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "cd frontend && npm install && npx prisma generate && npm run build",
    "lint": "npm run lint --prefix frontend",
    "start": "npm run start --prefix frontend"
  }
}
