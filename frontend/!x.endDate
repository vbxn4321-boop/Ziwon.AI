const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const p = await prisma.supportProgram.findMany({ select: { category: true, endDate: true } }); const today = new Date(); today.setHours(0,0,0,0); const active = p.filter(x = 
